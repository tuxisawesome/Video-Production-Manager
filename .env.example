SECRET_KEY=change-me-to-a-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DOMAIN=localhost:8000
DB_BACKEND=sqlite
# PostgreSQL settings (only if DB_BACKEND=postgres)
DB_NAME=vpm
DB_USER=vpm
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
# Defaults
DEFAULT_MAX_RECORDING_SECONDS=300
DEFAULT_MAX_RECORDINGS_PER_PROJECT=0
# Redis
REDIS_URL=redis://127.0.0.1:6379/0
